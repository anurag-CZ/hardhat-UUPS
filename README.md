# Upgradeable Contract using UUPS


Initialize project by:

```shell
npm i
npx hardhat copile
npx hardhat run scripts/deploy.js --network network-name
npx hardhat verify --network network-name contract-address
```
